pipeline {
//Pipeline, para llamar el proceso complero del Pipeline
    agent any
    stages{
        stage('LLamando del Pipeline externo')
            steps{
                script {
                    load 'https://github.com/edansanmar/devops.git/vars/pipelineGeneral.groovy'
                }
            }
    }
}